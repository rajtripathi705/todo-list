# Simple Todo List

Open [index.html](index.html) in your browser to use the app. Tasks are saved in your browser's `localStorage`.

Usage:

- Type a task and press `Add` or `Enter`.
- Click the checkbox to mark complete.
- Click the ✕ button to delete a task.
